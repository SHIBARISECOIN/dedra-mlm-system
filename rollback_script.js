const https = require('https');
const crypto = require('crypto');

// I will write a script to do it securely via REST API without touching index.tsx, 
// to be safe. Actually, I have full access to getAdminToken from inside the app if I just import it or just use the googleapis directly.
